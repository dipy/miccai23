

def show_data():
    # from dipy.viz import window, actor
    # scene = window.Scene()
    # scene.add(actor.line(target_streamlines, window.colors.red))
    # scene.add(actor.line(sft_atlas.streamlines, window.colors.green))
    # window.show(scene)

    # scene.clear()

    # scene.add(actor.line(moved, window.colors.red))
    # scene.add(actor.line(sft_atlas.streamlines, window.colors.green))
    # window.show(scene)
    pass